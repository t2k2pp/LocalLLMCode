# LocalLLM Code アーキテクチャ図

## 1. システム全体アーキテクチャ

### 1.1 高レベルアーキテクチャ

```mermaid
graph TB
    subgraph "External Environment"
        USER[👤 Developer]
        LMSTUDIO[🤖 LM Studio<br/>localhost:1234]
        AZURE[☁️ Azure ChatGPT]
        GEMINI[🔷 Google Gemini]
        GIT[📁 Git Repository]
        FS[💾 File System]
    end
    
    subgraph "LocalLLM Code System"
        subgraph "Presentation Layer"
            CLI[🖥️ Rich Console Interface]
            INTERACT[⚡ Interactive Commands]
        end
        
        subgraph "Application Layer"
            MAIN[🎯 LocalLLMCode Main]
            SESSION[📋 Session Management]
            CONFIG[⚙️ Configuration Manager]
        end
        
        subgraph "Agent Layer"
            REACT[🧠 ReAct Agent<br/>Think-Act-Observe]
            MULTI[👥 Multi-Agent System<br/>三人文殊 & 親分]
        end
        
        subgraph "Service Layer"
            TOOLS[🔧 Tool System<br/>Safe Operations]
            CONTEXT[📊 Smart Context Manager]
            MEMORY[🧠 External Memory System]
            DNA[🔬 Project DNA Analyzer]
        end
        
        subgraph "Integration Layer"
            LLM[🔌 LLM Client<br/>Multi-Provider]
            SAFETY[🛡️ Safety System]
            I18N[🌐 I18n Manager<br/>JP/EN]
        end
        
        subgraph "Infrastructure Layer"
            LOG[📝 Logging System]
            CACHE[⚡ Cache Manager]
            ERROR[❌ Error Handler]
        end
    end
    
    %% External connections
    USER <--> CLI
    LLM <--> LMSTUDIO
    LLM <--> AZURE
    LLM <--> GEMINI
    TOOLS <--> GIT
    TOOLS <--> FS
    
    %% Internal connections
    CLI --> MAIN
    INTERACT --> SESSION
    MAIN --> REACT
    REACT --> MULTI
    REACT --> TOOLS
    REACT --> CONTEXT
    REACT --> MEMORY
    TOOLS --> DNA
    TOOLS --> LLM
    TOOLS --> SAFETY
    MAIN --> CONFIG
    MAIN --> I18N
    MAIN --> LOG
    CONTEXT --> CACHE
    SAFETY --> ERROR
```

## 2. データフローアーキテクチャ

### 2.1 情報処理フロー

```mermaid
graph LR
    subgraph "Input Processing"
        INPUT[User Input] --> PARSE[Query Parser]
        PARSE --> REF[File Reference<br/>@filename]
        REF --> LOAD[Auto File Load]
    end
    
    subgraph "Context Assembly"
        LOAD --> CONTEXT[Context Builder]
        CONTEXT --> COMPRESS[Context Compression]
        COMPRESS --> OPTIMIZE[Token Optimization]
    end
    
    subgraph "ReAct Processing"
        OPTIMIZE --> THINK[Think Phase]
        THINK --> ACT[Act Phase]
        ACT --> OBSERVE[Observe Phase]
        OBSERVE --> LOOP{Continue Loop?}
        LOOP -->|Yes| THINK
        LOOP -->|No| RESULT[Final Result]
    end
    
    subgraph "Output Processing"
        RESULT --> FORMAT[Response Formatting]
        FORMAT --> DISPLAY[Rich Display]
        DISPLAY --> MEMORY[Memory Update]
    end
    
    subgraph "Parallel Processes"
        SAFETY[Safety Monitoring]
        HEALTH[Health Checking]
        BACKUP[Auto Backup]
    end
    
    ACT -.-> SAFETY
    THINK -.-> HEALTH
    ACT -.-> BACKUP
```

## 3. ReActエージェント詳細アーキテクチャ

### 3.1 ReActループアーキテクチャ

```mermaid
graph TD
    subgraph "ReAct Agent Core"
        START[Query Input] --> INIT[Initialize Context]
        
        subgraph "Think Phase"
            INIT --> ANALYZE[Situation Analysis]
            ANALYZE --> PLAN[Action Planning]
            PLAN --> REASON[Reasoning]
        end
        
        subgraph "Act Phase"
            REASON --> SELECT[Tool Selection]
            SELECT --> VALIDATE[Safety Validation]
            VALIDATE --> EXECUTE[Tool Execution]
        end
        
        subgraph "Observe Phase"
            EXECUTE --> COLLECT[Result Collection]
            COLLECT --> INTERPRET[Result Interpretation]
            INTERPRET --> LEARN[Learning Update]
        end
        
        subgraph "Loop Control"
            LEARN --> DETECT[Loop Detection]
            DETECT --> CHECK{Max Iterations?}
            CHECK -->|No| STUCK{Stuck Pattern?}
            STUCK -->|No| ANALYZE
            STUCK -->|Yes| CONSULT[Multi-Agent Consult]
            CONSULT --> RESOLVE[Resolution Strategy]
            RESOLVE --> ANALYZE
            CHECK -->|Yes| COMPLETE[Complete Task]
        end
        
        COMPLETE --> MEMORY[Update Memory]
        MEMORY --> OUTPUT[Return Result]
    end
    
    subgraph "Supporting Systems"
        CONTEXT_MGR[Context Manager]
        TOOL_SYS[Tool System]
        MULTI_AGENT[Multi-Agent System]
        EXT_MEMORY[External Memory]
    end
    
    ANALYZE <--> CONTEXT_MGR
    EXECUTE <--> TOOL_SYS
    CONSULT <--> MULTI_AGENT
    LEARN <--> EXT_MEMORY
```

## 4. マルチエージェントシステムアーキテクチャ

### 4.1 協議システム構造

```mermaid
graph TD
    subgraph "Multi-Agent Consultation System"
        TRIGGER[Consultation Trigger] --> TYPE{Consultation Type}
        
        subgraph "Three Wise Agents (三人文殊)"
            TYPE -->|Normal Problem| DISPATCH[Agent Dispatch]
            DISPATCH --> CONSERVATIVE[🛡️ Conservative Agent<br/>Risk Assessment]
            DISPATCH --> PROGRESSIVE[🚀 Progressive Agent<br/>Optimization Focus]
            DISPATCH --> JUDGE[⚖️ Judge Agent<br/>Balanced Decision]
            
            CONSERVATIVE --> SYNTHESIS[Opinion Synthesis]
            PROGRESSIVE --> SYNTHESIS
            JUDGE --> SYNTHESIS
        end
        
        subgraph "Boss Consultation (親分呼び出し)"
            TYPE -->|Complex Problem| ESCALATE[Problem Escalation]
            ESCALATE --> BOSS[👑 Boss Agent<br/>Senior AI Wisdom]
            BOSS --> GUIDANCE[Strategic Guidance]
        end
        
        subgraph "Decision Making"
            SYNTHESIS --> EVALUATE[Evaluation Matrix]
            GUIDANCE --> EVALUATE
            EVALUATE --> DECISION[Final Decision]
            DECISION --> CONFIDENCE[Confidence Score]
        end
        
        subgraph "Quality Control"
            CONFIDENCE --> THRESHOLD{Confidence > 80%?}
            THRESHOLD -->|No| RETRY[Retry with Boss]
            RETRY --> ESCALATE
            THRESHOLD -->|Yes| RESULT[Return Result]
        end
    end
    
    subgraph "Provider Management"
        HEALTH[Health Monitor] --> AVAILABLE[Available Providers]
        RATE[Rate Limiter] --> SCHEDULE[Request Scheduling]
        FALLBACK[Fallback Strategy] --> BACKUP[Backup Providers]
    end
    
    DISPATCH <--> AVAILABLE
    ESCALATE <--> SCHEDULE
    BOSS <--> BACKUP
```

## 5. ツールシステムアーキテクチャ

### 5.1 安全性重視ツール実行

```mermaid
graph TD
    subgraph "Tool Execution Pipeline"
        REQUEST[Tool Request] --> VALIDATE[Input Validation]
        
        subgraph "Safety Layer"
            VALIDATE --> PATH[Path Validation]
            PATH --> COMMAND[Command Safety Check]
            COMMAND --> PATTERN[Dangerous Pattern Detection]
        end
        
        subgraph "Authorization Layer"
            PATTERN --> RISK[Risk Assessment]
            RISK --> CONFIRM{Need Confirmation?}
            CONFIRM -->|Yes| USER[User Confirmation]
            USER -->|Denied| REJECT[Operation Rejected]
            USER -->|Approved| PROCEED[Proceed with Caution]
            CONFIRM -->|No| PROCEED
        end
        
        subgraph "Execution Layer"
            PROCEED --> BACKUP[Create Backup]
            BACKUP --> EXECUTE[Safe Execution]
            EXECUTE --> MONITOR[Execution Monitoring]
        end
        
        subgraph "Verification Layer"
            MONITOR --> VERIFY[Result Verification]
            VERIFY --> DETECT[Destruction Detection]
            DETECT --> LOG[Operation Logging]
        end
        
        LOG --> SUCCESS[Return Success]
        REJECT --> FAILURE[Return Failure]
    end
    
    subgraph "Tool Registry"
        FILES[📁 File Operations]
        COMMANDS[⚡ Command Execution]
        GIT_OPS[📋 Git Operations]
        ANALYSIS[🔍 Code Analysis]
    end
    
    subgraph "Safety Mechanisms"
        SANDBOX[🏗️ Sandboxing]
        WHITELIST[✅ Command Whitelist]
        BLACKLIST[❌ Dangerous Commands]
        RECOVERY[🔄 Auto Recovery]
    end
    
    EXECUTE <--> FILES
    EXECUTE <--> COMMANDS
    EXECUTE <--> GIT_OPS
    EXECUTE <--> ANALYSIS
    
    VALIDATE <--> SANDBOX
    COMMAND <--> WHITELIST
    PATTERN <--> BLACKLIST
    MONITOR <--> RECOVERY
```

## 6. LLM統合アーキテクチャ

### 6.1 マルチプロバイダー通信システム

```mermaid
graph TD
    subgraph "LLM Client Architecture"
        subgraph "Unified Interface"
            API[Unified LLM API] --> ROUTER[Provider Router]
        end
        
        subgraph "Provider Implementations"
            ROUTER --> LMSTUDIO_CLIENT[LM Studio Client<br/>localhost:1234]
            ROUTER --> AZURE_CLIENT[Azure ChatGPT Client<br/>Custom Endpoint]
            ROUTER --> GEMINI_CLIENT[Gemini Client<br/>Google API]
        end
        
        subgraph "Connection Management"
            LMSTUDIO_CLIENT --> POOL1[Connection Pool]
            AZURE_CLIENT --> POOL2[Connection Pool]
            GEMINI_CLIENT --> POOL3[Connection Pool]
        end
        
        subgraph "Quality Assurance"
            POOL1 --> HEALTH1[Health Monitor]
            POOL2 --> HEALTH2[Health Monitor]
            POOL3 --> HEALTH3[Health Monitor]
            
            HEALTH1 --> SELECTOR[Provider Selector]
            HEALTH2 --> SELECTOR
            HEALTH3 --> SELECTOR
        end
        
        subgraph "Resilience Features"
            SELECTOR --> RETRY[Auto Retry Logic]
            RETRY --> FALLBACK[Fallback Strategy]
            FALLBACK --> CIRCUIT[Circuit Breaker]
        end
        
        CIRCUIT --> RESPONSE[Unified Response]
    end
    
    subgraph "External Services"
        LMSTUDIO[🤖 LM Studio]
        AZURE[☁️ Azure ChatGPT]
        GEMINI[🔷 Google Gemini]
    end
    
    LMSTUDIO_CLIENT <--> LMSTUDIO
    AZURE_CLIENT <--> AZURE
    GEMINI_CLIENT <--> GEMINI
```

## 7. メモリシステムアーキテクチャ

### 7.1 外部メモリとコンテキスト管理

```mermaid
graph TD
    subgraph "External Memory System"
        subgraph "Memory Storage"
            TODO[📋 TODO Management]
            SESSION[📝 Session Records]
            CONTEXT_CACHE[💾 Context Cache]
            PATTERNS[🔍 Usage Patterns]
        end
        
        subgraph "Task Management"
            TODO --> CHUNKER[Task Chunker]
            CHUNKER --> PRIORITY[Priority Queue]
            PRIORITY --> SCHEDULER[Task Scheduler]
        end
        
        subgraph "Context Optimization"
            SESSION --> RELEVANCE[Relevance Scorer]
            CONTEXT_CACHE --> COMPRESSOR[Context Compressor]
            RELEVANCE --> SELECTOR[Smart File Selector]
            COMPRESSOR --> OPTIMIZER[Token Optimizer]
        end
        
        subgraph "Learning System"
            PATTERNS --> ANALYZER[Pattern Analyzer]
            ANALYZER --> PREDICTOR[Preference Predictor]
            PREDICTOR --> RECOMMENDER[Context Recommender]
        end
        
        subgraph "Persistence Layer"
            SCHEDULER --> LOCALLLM_MD[LOCALLLM.md]
            OPTIMIZER --> PROJECT_DNA[Project DNA]
            RECOMMENDER --> USER_PREFS[User Preferences]
        end
    end
    
    subgraph "Smart Context Manager"
        TOKEN_MONITOR[Token Monitor] --> WARNING[Context Warning]
        COMPRESSION[Auto Compression] --> REDUCTION[Context Reduction]
        SELECTION[File Selection] --> RANKING[Relevance Ranking]
    end
    
    SELECTOR <--> TOKEN_MONITOR
    COMPRESSOR <--> COMPRESSION
    RELEVANCE <--> SELECTION
```

## 8. セキュリティアーキテクチャ

### 8.1 多層防御システム

```mermaid
graph TD
    subgraph "Multi-Layer Security Architecture"
        subgraph "Input Security"
            INPUT[User Input] --> SANITIZE[Input Sanitization]
            SANITIZE --> VALIDATE[Input Validation]
            VALIDATE --> ESCAPE[Special Character Escape]
        end
        
        subgraph "Path Security"
            ESCAPE --> PATH_CHECK[Path Traversal Check]
            PATH_CHECK --> PROJECT_BOUND[Project Boundary Check]
            PROJECT_BOUND --> PERMISSION[Permission Validation]
        end
        
        subgraph "Command Security"
            PERMISSION --> CMD_FILTER[Command Filter]
            CMD_FILTER --> WHITELIST[Whitelist Check]
            WHITELIST --> BLACKLIST[Blacklist Check]
            BLACKLIST --> PATTERN[Malicious Pattern Detection]
        end
        
        subgraph "Execution Security"
            PATTERN --> SANDBOX[Sandboxed Execution]
            SANDBOX --> MONITOR[Runtime Monitoring]
            MONITOR --> LIMIT[Resource Limits]
            LIMIT --> TIMEOUT[Timeout Control]
        end
        
        subgraph "Data Security"
            TIMEOUT --> BACKUP[Auto Backup]
            BACKUP --> ENCRYPT[Data Encryption]
            ENCRYPT --> AUDIT[Audit Logging]
        end
        
        AUDIT --> SECURE_RESULT[Secure Result]
    end
    
    subgraph "Security Components"
        FIREWALL[🔥 Command Firewall]
        DETECTOR[🕵️ Threat Detector]
        ISOLATOR[🏝️ Process Isolator]
        LOGGER[📊 Security Logger]
    end
    
    CMD_FILTER <--> FIREWALL
    PATTERN <--> DETECTOR
    SANDBOX <--> ISOLATOR
    AUDIT <--> LOGGER
```

## 9. エラー処理とレジリエンスアーキテクチャ

### 9.1 包括的エラー処理システム

```mermaid
graph TD
    subgraph "Error Handling Architecture"
        subgraph "Error Detection"
            OPERATION[Operation] --> ERROR[Error Occurs]
            ERROR --> CLASSIFY[Error Classification]
            CLASSIFY --> SEVERITY[Severity Assessment]
        end
        
        subgraph "Recovery Strategy"
            SEVERITY --> STRATEGY{Recovery Strategy}
            STRATEGY -->|Retry| BACKOFF[Exponential Backoff]
            STRATEGY -->|Fallback| ALTERNATIVE[Alternative Approach]
            STRATEGY -->|Escalate| HUMAN[Human Intervention]
            STRATEGY -->|Abort| CLEANUP[Graceful Cleanup]
        end
        
        subgraph "Retry Logic"
            BACKOFF --> ATTEMPT[Retry Attempt]
            ATTEMPT --> SUCCESS{Success?}
            SUCCESS -->|No| COUNT{Max Retries?}
            COUNT -->|No| BACKOFF
            COUNT -->|Yes| ALTERNATIVE
            SUCCESS -->|Yes| RECOVER[Recovery Complete]
        end
        
        subgraph "Fallback System"
            ALTERNATIVE --> PROVIDER[Switch Provider]
            PROVIDER --> DEGRADED[Degraded Mode]
            DEGRADED --> BASIC[Basic Functionality]
        end
        
        subgraph "Monitoring & Logging"
            BASIC --> LOG[Error Logging]
            RECOVER --> LOG
            CLEANUP --> LOG
            LOG --> ALERT[Alert System]
            ALERT --> METRICS[Error Metrics]
        end
    end
    
    subgraph "Resilience Components"
        CIRCUIT[🔌 Circuit Breaker]
        BULKHEAD[🚢 Bulkhead Pattern]
        CACHE[⚡ Cache Fallback]
        QUEUE[📬 Message Queue]
    end
    
    PROVIDER <--> CIRCUIT
    DEGRADED <--> BULKHEAD
    ALTERNATIVE <--> CACHE
    ALERT <--> QUEUE
```

## 10. 配置とスケーラビリティ

### 10.1 システム配置アーキテクチャ

```mermaid
graph TD
    subgraph "Development Environment"
        DEV_MACHINE[💻 Developer Machine]
        LOCAL_LLM[🤖 Local LLM Studio]
        DEV_TOOLS[🔧 Development Tools]
    end
    
    subgraph "LocalLLM Code Instance"
        MAIN_APP[📱 Main Application]
        CONFIG_MGR[⚙️ Configuration Manager]
        PLUGIN_SYS[🔌 Plugin System]
    end
    
    subgraph "External LLM Services"
        AZURE_CLOUD[☁️ Azure Cloud]
        GOOGLE_CLOUD[🔷 Google Cloud]
        LOCAL_MODELS[🏠 Local Models]
    end
    
    subgraph "Project Environment"
        PROJECT_DIR[📁 Project Directory]
        GIT_REPO[📋 Git Repository]
        CONFIG_FILES[📄 Config Files]
        MEMORY_FILES[🧠 Memory Files]
    end
    
    subgraph "Monitoring & Logging"
        LOG_FILES[📝 Log Files]
        METRICS[📊 Metrics]
        HEALTH_CHECK[❤️ Health Monitoring]
    end
    
    DEV_MACHINE --> MAIN_APP
    LOCAL_LLM --> MAIN_APP
    DEV_TOOLS --> MAIN_APP
    
    MAIN_APP <--> AZURE_CLOUD
    MAIN_APP <--> GOOGLE_CLOUD
    MAIN_APP <--> LOCAL_MODELS
    
    MAIN_APP <--> PROJECT_DIR
    MAIN_APP <--> GIT_REPO
    CONFIG_MGR <--> CONFIG_FILES
    MAIN_APP <--> MEMORY_FILES
    
    MAIN_APP --> LOG_FILES
    MAIN_APP --> METRICS
    MAIN_APP --> HEALTH_CHECK
```

## 11. パフォーマンス最適化アーキテクチャ

### 11.1 性能最適化システム

```mermaid
graph TD
    subgraph "Performance Optimization Architecture"
        subgraph "Request Processing"
            REQUEST[Request] --> CACHE_CHECK[Cache Check]
            CACHE_CHECK -->|Hit| CACHE_RESULT[Cache Result]
            CACHE_CHECK -->|Miss| PROCESS[Process Request]
        end
        
        subgraph "Parallel Processing"
            PROCESS --> DECOMPOSE[Task Decomposition]
            DECOMPOSE --> PARALLEL[Parallel Execution]
            PARALLEL --> AGGREGATE[Result Aggregation]
        end
        
        subgraph "Resource Management"
            AGGREGATE --> POOL[Connection Pool]
            POOL --> QUEUE[Request Queue]
            QUEUE --> SCHEDULER[Load Scheduler]
        end
        
        subgraph "Optimization Strategies"
            SCHEDULER --> COMPRESS[Context Compression]
            COMPRESS --> BATCH[Request Batching]
            BATCH --> PREDICT[Predictive Loading]
        end
        
        subgraph "Monitoring"
            PREDICT --> MONITOR[Performance Monitor]
            MONITOR --> METRICS[Performance Metrics]
            METRICS --> TUNING[Auto Tuning]
        end
        
        TUNING --> ADJUSTMENT[Dynamic Adjustment]
        CACHE_RESULT --> RESPONSE[Response]
        ADJUSTMENT --> RESPONSE
    end
    
    subgraph "Caching Strategy"
        L1_CACHE[🚀 L1: Memory Cache]
        L2_CACHE[💾 L2: File Cache]
        L3_CACHE[🌐 L3: Remote Cache]
    end
    
    CACHE_CHECK <--> L1_CACHE
    CACHE_CHECK <--> L2_CACHE
    CACHE_CHECK <--> L3_CACHE
```

これらのアーキテクチャ図は、LocalLLM Code システムの包括的な設計と実装を視覚化しており、開発者がシステムの構造、データフロー、セキュリティ、パフォーマンス最適化を理解するのに役立ちます。